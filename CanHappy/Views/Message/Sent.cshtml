@model IEnumerable<CanHappy.Models.Messaging.MessageListItemViewModel>

@{
    ViewData["Title"] = "Sent";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="mb-0">Sent</h1>
    <a asp-action="Inbox" class="btn btn-outline-secondary">Inbox</a>
</div>

@if (!Model.Any())
{
    <div class="alert alert-info">No sent messages.</div>
}
else
{
    <div class="list-group">
        @foreach (var message in Model)
        {
            <a asp-action="Details" asp-route-id="@message.UserMessageGUID" class="list-group-item list-group-item-action">
                <div class="d-flex w-100 justify-content-between align-items-start">
                    <div>
                        <h5 class="mb-1">@message.Subject</h5>
                        <p class="mb-1 text-muted">@message.BodyPreview</p>
                        <small>
                            To: <strong>@message.CounterpartyName</strong> |
                            Listing: <a asp-controller="ListingPage" asp-action="Details"
                                        asp-route-id="@message.ListingGUID">@message.ListingSubject</a>
                        </small>
                    </div>
                    <div class="text-end">
                        <div><small>@message.CreatedDate.ToLocalTime()</small></div>
                    </div>
                </div>
            </a>
        }
    </div>
}